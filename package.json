{
  "name": "abstraction-account-environment",
  "version": "1.0.0",
  "description": "ERC-4337 Account Abstraction local test environment",
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.ts --network localhost",
    "bundler": "ts-node bundler/index.ts",
    "test": "hardhat test",
    "full-flow": "hardhat run scripts/run-full-flow.ts --network localhost",
    "infra-test": "hardhat run scripts/send-via-bundler.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@account-abstraction/contracts": "^0.7.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.13.0",
    "express": "^4.18.0"
  }
}
